# Sample Spec — Checkout Flow

This file is a placeholder. In a real pm-eval run, drop a generated spec here
(e.g., output from `ears-spec-agent`) and call:

```bash
pm-eval grade -i examples/sample_spec.md -r rubrics/spec-quality.yaml
```

A real example will land here when v0.1 ships.
